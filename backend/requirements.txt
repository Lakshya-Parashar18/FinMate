# =============================================================================
# FinMate AI Service — Consolidated Python Requirements
# This is the single authoritative requirements file.
# The Dockerfile and CI/CD pipelines reference this file only.
#
# Sub-module requirements files (categorization/, dataset_generator/) are kept
# for standalone local development within those modules but are not used in
# production builds.
# =============================================================================

# --- Web Framework ---
fastapi>=0.95.0
uvicorn>=0.20.0
pydantic>=1.10.0,<2.0.0

# --- Data & ML ---
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.2.0
xgboost>=1.7.0
scipy>=1.10.0
joblib>=1.2.0

# --- Embeddings & NLP ---
sentence-transformers>=2.2.2
torch>=2.0.0

# --- Database ---
pymongo>=4.3.0
dnspython>=2.3.0

# --- Utilities ---
tqdm>=4.65.0
python-dotenv>=1.0.0

# --- Data Generation (dataset_generator module) ---
faker>=18.0.0

# --- Dev / Testing (not needed in production Docker image) ---
# To install without dev deps: pip install -r requirements.txt --ignore-requires-python
pytest>=7.3.0
