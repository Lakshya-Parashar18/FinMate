# =============================================================================
# FinMate AI Service — Consolidated Python Requirements (ONNX Optimized)
# Single authoritative requirements file optimized for low-resource environments (e.g. Render Free Tier).
# Eliminates massive PyTorch (torch) and sentence-transformers to stay under 150MB RAM.
# =============================================================================

# --- Web Framework ---
fastapi>=0.95.0
uvicorn>=0.20.0
pydantic>=1.10.0,<2.0.0

# --- Data & ML ---
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.2.0
xgboost>=1.7.0
scipy>=1.10.0
joblib>=1.2.0

# --- Embeddings & NLP (ONNX Optimized for Free Tier) ---
onnxruntime>=1.15.0
tokenizers>=0.13.0
huggingface-hub>=0.14.0

# --- Database ---
pymongo>=4.3.0
dnspython>=2.3.0

# --- Utilities ---
tqdm>=4.65.0
python-dotenv>=1.0.0

# --- Data Generation (dataset_generator module) ---
faker>=18.0.0

# --- Dev / Testing (not needed in production Docker image) ---
pytest>=7.3.0
